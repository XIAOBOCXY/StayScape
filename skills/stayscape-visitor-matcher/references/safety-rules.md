# Safety rules

- Allergy information is a warning and a handoff, never a safety guarantee.
- Child ages must be checked against each experience's minimum and maximum age.
- If no product passes hard constraints, return an empty result and explain which constraint blocked the match.

